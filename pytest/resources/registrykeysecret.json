{
    "kind": "Secret",
    "apiVersion": "v1",
    "metadata": {
        "name": "myregistrykey",
        "namespace": "{{env.namespace}}"
    },
    "data": {
        ".dockerconfigjson": "{{env.dockerconfigjson}}"
    },
    "type": "kubernetes.io/dockerconfigjson"
}

