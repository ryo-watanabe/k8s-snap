{
    "kind": "Secret",
    "apiVersion": "v1",
    "metadata": {
        "name": "{{env.secret}}",
        "namespace": "{{env.namespace}}"
    },
    "data": {
        "accesskey": "{{env.accesskey_encoded}}",
        "secretkey": "{{env.secretkey_encoded}}"
    },
    "type": "Opaque"
}

